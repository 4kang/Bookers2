<%= render 'list/lists', book: @book, user: @user %>

<div class="col-md-8 offset-md-1">
 <h3>Books</h3>
 <table class="table table-hover table-inverse">
  <thead>
   <tr>
    <th>Title</th>
    <th>Opinion</th>
    <th colspan="3"></th>
   </tr>
  </thead>
  <tbody>
   <tr>
    <td>

    </td>
   </tr>
  </tbody>
 </table>
</div>



<% @books.each do |book| %>
 <div>
  <%= link_to book.title, book_path(book.id) %>

   <p><%= book.body %></p>
 </div>
<% end %>