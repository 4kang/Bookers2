<h1>Editing Book</h1>
<div>
<%= form_with model: @book do |f| %>

<p>title</p>
<%= f.text_field :title %>

<p>opinion</p>
<%= f.text_area :body %>

<%= f.submit 'Update Book' %>
</div>
<% end %>

<%= link_to "Show", book_path(@book.id) %>
|
<%= link_to "Back", '/books' %>

