<h2>Book detail</h2>

<%= @book.title %>
<%= @book.body %>

<% if @book.user == current_user %>
 <%= link_to "Edit", edit_book_path(@book.id) %>
 <%= link_to "Destroy", books_path(@book), method: :delete %>
<% end %>



<%= render 'list/lists', book: @new_book, user: @user %>